-- Figure 2: MFA Bypass Detection
-- Purpose: Identify MFA bypass attempts linked to attacker IP

index=final_project sourcetype=mfa user=BDRVLS
| search action="bypass" OR status="bypassed"
| table _time user src_ip application action
| sort _time
